<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/manageConferencePanel"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingTop="@dimen/freeme_conference_call_manager_padding_top">

    <ListView
        android:id="@+id/participantList"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:divider="@null"
        android:focusable="true"
        android:focusableInTouchMode="true"
        android:listSelector="@null" />

    <View
        android:layout_width="match_parent"
        android:layout_height="1dp"
        android:background="@color/freeme_list_divider_color"/>

    <FrameLayout
        android:id="@+id/floating_add_member_action_button_container"
        android:layout_width="@dimen/floating_action_button_width"
        android:layout_height="@dimen/floating_action_button_width"
        android:layout_alignParentBottom="true"
        android:layout_alignParentRight="true"
        android:layout_marginBottom="@dimen/floating_action_button_margin_bottom"
        android:layout_marginRight="@dimen/floating_action_button_margin_bottom"
        android:visibility="gone">

        <ImageButton
            android:id="@+id/floating_add_member_action_button"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:background="@drawable/mtk_volte_ic_conference_add_member_bkg"
            android:contentDescription="@string/addConferenceMemeberText"
            android:src="@drawable/mtk_volte_ic_conference_add_member"
            android:tint="@android:color/white" />
    </FrameLayout>

</RelativeLayout>